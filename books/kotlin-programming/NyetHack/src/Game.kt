fun main(args: Array<String>) {
    var name = "마드리갈"
    var healthPoints = 100

    if (healthPoints == 100) {
        println(name + " 최상의 상태임!")
    } else {
        println(name + " 최악의 상태임!")
    }
}